let myLibrary = []

function Book(title, author, pages, status){
  this.id = myLibrary.length;
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.status = status;
};

function addBookToLibrary(){
  myLibrary.push(
    new Book(
      
      )
    )
}
